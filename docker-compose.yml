

services:
  web:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    command: >
       sh -c "python App/manage.py migrate && gunicorn --bind 0.0.0.0:8000 app.wsgi:application"